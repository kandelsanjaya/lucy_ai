# Copy this file to `.streamlit/secrets.toml` and fill in your real keys.
# NEVER commit the real secrets.toml to git.

OPENAI_API_KEY = "sk-..."

# Optional — enables live results in the Web Search tool
TAVILY_API_KEY = ""
